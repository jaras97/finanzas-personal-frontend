# Arquitectura

## Routing y protección de rutas

**Grupos de rutas** (App Router):
- `src/app/(app)/` — shell autenticado: `summary`, `transactions`, `recurring`, `saving-accounts`, `debts`, `categories`, `budgets`, `import`, `rules`, `admin`. Tiene su propio `layout.tsx` que renderiza `Sidebar` + `Footer` y hace **gating de suscripción del lado del cliente**.
- `src/app/auth/` — `login`, `expired`, `inactive`, `no-subscription`. Fuera de `(app)`, sin sidebar/footer.

No existe `src/app/page.tsx`: la ruta `/` la resuelve `src/middleware.ts`, que siempre redirige.

**Navegación** (`Sidebar.tsx`, reagrupada 2026-08-30): Resumen y Transacciones sueltos arriba (uso diario, sin sección propia); grupo PATRIMONIO (Cuentas, Deudas); grupo AJUSTES (Categorías, Mi cuenta, y Usuarios solo para admins). `/recurring` sigue existiendo como ruta propia y protegida igual que antes, pero ya no tiene ítem en el sidebar — se presenta como pestaña dentro de `/transactions` vía `TransactionsTabs.tsx` (`components/layout/`), un segmented-control liviano que solo navega entre las dos rutas (no fusiona su estado/lógica interna). Mismo patrón para `/budgets` (desde 2026-08-30): sin ítem propio en el sidebar, se presenta como pestaña "Presupuestos" dentro de `/categories` vía `CategoriesTabs.tsx`.

**`src/middleware.ts`** (Edge runtime):
- Lee la cookie `access_token` y la verifica con `jose.jwtVerify` usando `process.env.JWT_SECRET` (debe ser igual al `SECRET_KEY` del backend).
- Rutas privadas (por prefijo): `/summary`, `/transactions`, `/recurring`, `/saving-accounts`, `/categories`, `/budgets`, `/import`, `/rules`, `/debts`, `/admin` → sin token o token inválido, redirige a `/auth/login`. El middleware solo verifica que haya **sesión válida**; que además sea admin lo valida el backend (403) y la propia página `/admin` al montar.
- En `/`: token válido → `/summary`; si no, → `/auth/login`.
- En `/auth/login`: token válido → redirige a `/summary` (evita mostrar login ya autenticado).
- ⚠️ `/auth/expired`, `/auth/inactive`, `/auth/no-subscription` están en el `matcher` pero **no tienen lógica de protección dentro del middleware** — al vivir fuera de `(app)`, quedan libremente accesibles sin ningún control (la protección real de esas pantallas depende solo del gating de suscripción del layout `(app)`, que no aplica a rutas fuera de ese grupo).
- Contiene bastante `console.log` de debug (con emojis) en cada rama — dejado en el código, no es solo de desarrollo.

**Gating de suscripción** (`src/app/(app)/layout.tsx`, client component):
- Llama a `useSubscriptionStatus()` **y** `useCurrentUser()` al montar. Mientras alguno no ha resuelto, muestra "Verificando suscripción..." sin sidebar.
- **Los administradores se saltan el gate por completo** (`isAdmin` → nunca se bloquea ni redirige). Son personal, no clientes: no tienen por qué tener suscripción propia, y sin esta excepción un admin sin suscripción quedaba bloqueado fuera del panel que justamente usa para otorgarlas. Coincide con el backend, donde `get_current_admin_user` nunca validó suscripción.
- Si `sessionStorage['fromLogin'] === '1'` (seteado por `LoginForm` justo tras login exitoso), redirige **silenciosamente** (sin toast) a la pantalla de suscripción correspondiente y limpia el flag.
- En cualquier otro caso (deep link, cambio de estado en caliente), muestra un `toast.error` y redirige — solo una vez por montaje (`didToast` ref).
- Si el estado no es válido, el layout renderiza `null` mientras el `useEffect` async decide el redirect — puede haber un flash de contenido en blanco.

## Autenticación

Reescrito por completo el 2026-08-22: la sesión ya no vive en el cliente. El backend fija una cookie httpOnly (`access_token`, dominio `.balancedcent.com` en producción) en `/auth/login` y la limpia en `/auth/logout`; el frontend nunca lee ni guarda el JWT.

**Cliente HTTP** (`src/lib/api.ts`):
- `baseURL = NEXT_PUBLIC_API_URL` (default `http://localhost:8000`; en producción apunta a `https://api.balancedcent.com`); fuerza `https://` si la URL no es `localhost`.
- `withCredentials: true` — es lo único que hace falta para que el navegador adjunte la cookie en cada request. No hay interceptor de request ni de response: no se lee ni escribe ningún token, no hay refresh automático ni redirect global en 401 (cada hook sigue manejando sus errores por su cuenta).
- Exporta `logout()`: hace `POST /auth/logout` (best-effort, ignora errores de red) para que el backend limpie la cookie — necesario porque JS no puede borrar una cookie httpOnly por sí mismo. Los 4 lugares que antes duplicaban la lógica de logout (`Header.tsx`, `Sidebar.tsx`, `auth/expired|inactive|no-subscription`) ahora solo llaman a este helper + `router.push('/auth/login')`.

**Login** (`LoginForm.tsx`): `POST /auth/login` (form-urlencoded, estilo OAuth2). El frontend no toca la respuesta más allá de leer el status — la cookie la fija el backend vía `Set-Cookie`. Setea `sessionStorage['fromLogin']='1'` y navega a `/summary`.

**Registro** (`RegisterForm.tsx`): `POST /auth/register` `{email, password}`. **No hay auto-login** tras registrarse — el usuario vuelve al tab de login. El mensaje de éxito indica que la cuenta necesita una suscripción activa (aprovisionamiento manual/admin, no self-serve).

**Recuperar contraseña** (`ForgotPasswordForm.tsx`): `POST /auth/forgot-password`. El tab correspondiente en `AuthPanel.tsx` está **comentado en la UI** (no se puede llegar a este formulario desde la interfaz actual salvo que se reactive el botón).

**Estado de suscripción** (`useSubscriptionStatus.ts`): `GET /subscriptions/me`. Prioridad de estado: `end_date` vencida → `expired` (incluso si `is_active` es true) → `!is_active` → `inactive` → vence en ≤7 días → `expiring_soon` → si no, `active`. 401/404 → `none`. Otros errores no degradan el estado (evita falsos negativos por errores transitorios), pero si el primer request falla con algo distinto de 401/404, el estado puede quedar atascado indefinidamente en `loading`.

**Logout**: centralizado en `logout()` (`src/lib/api.ts`), llamado desde `Header.tsx`, `Sidebar.tsx` y cada pantalla `auth/expired|inactive|no-subscription`. Ya no hay lógica duplicada ni token que limpiar del lado del cliente.

> ⚠️ Nota de migración: cualquier usuario que ya tuviera una sesión iniciada antes de este cambio conserva la cookie **vieja** (`access_token`, no-httpOnly, con dominio scoped solo a `www.balancedcent.com` — la ponía el frontend antiguo vía `js-cookie`). Esa cookie sigue siendo válida para el middleware (que solo la lee, no le importa quién la puso), así que deja entrar al usuario a `(app)`, pero **nunca llega a `api.balancedcent.com`** (dominio distinto), así que toda llamada a la API sale sin credenciales → 401 → `useSubscriptionStatus` muestra "Suscripción pendiente" aunque la cuenta esté bien. Se resuelve solo con un logout+login (que limpia la cookie vieja y pone la nueva, correctamente scoped). Confirmado en producción el 2026-08-22.

## Estado y fetching de datos

- **Zustand**: solo `useSidebarStore` (sidebar móvil) — el antiguo `useAuthStore` (token en `localStorage`) se eliminó junto con el resto de la persistencia de sesión en el cliente (ver Autenticación arriba). No hay un store global de datos de dominio.
- **Sin caché de servidor compartida** (no React Query): cada feature tiene su propio hook `useState`+`useEffect`+axios con `loading`/`error`/`refresh()` manual. Única excepción: `useDebts.ts` usa **SWR** (revalida on focus/reconnect), inconsistente con el resto.
- **Formularios**: todos manuales (`useState` por campo + validación a mano + `toast.error`), pese a tener `react-hook-form`/`zod`/`form.tsx` instalados y sin usar.
- Los inputs de monto/tasa usan `react-number-format` (`NumericFormat`) para separadores de miles/decimales según locale.

## Fechas y monedas

Dos estrategias de parámetros de fecha coexisten (a tener en cuenta al tocar filtros):
- **Summary / cash-flow**: fechas simples `YYYY-MM-DD` como `start_date`/`end_date` (`src/lib/dateParams.ts`).
- **Transactions**: límites completos en UTC como `startDate`/`endDate` (camelCase), convertidos desde el día local del navegador (`toUtcDayBoundsFromISOStrings`), para que el filtrado respete el día calendario local, no UTC crudo.

El patrón "fecha a mediodía local en ISO" (`dateToIsoAtLocalNoon` / `toIsoAtLocalNoon`) para evitar corrimientos de día por timezone está **copiado en al menos 5 lugares** distintos (`utils/dates.ts`, `NewTransactionModal.tsx`, `EditTransactionModal.tsx`, `PayDebtModal.tsx`, `TransferBetweenAccountsModal.tsx`) en vez de centralizado.

## Monedas (reescrito 2026-08-22)

`currencyType` es `string` (antes un union cerrado `"COP" | "USD"`). El catálogo real viene de `GET /currencies` vía el hook `useCurrencies()` (`src/hooks/useCurrencies.ts`), que devuelve `{code, name, symbol, decimal_digits}[]` para las 42 monedas que el backend soporta. Está wireado en:
- Todos los selects de moneda al crear/editar cuentas y deudas (`NewSavingAccountModal`, `NewDebtModal`, `EditDebtModal`) — antes hardcodeados a solo COP/USD.
- Las heurísticas de escala decimal ("¿esta moneda usa centavos?") en `TransferBetweenAccountsModal`, `NewTransactionModal`, `PayDebtModal`, `AddChargeToDebtModal`, `RegisterYieldModal` — antes todas asumían `currency === 'COP' ? 0 : 2`, lo cual es incorrecto para cualquier otra moneda sin decimales (JPY, CLP, KRW, ...). Ahora leen `decimal_digits` del catálogo.
- `CurrencyToggle` (dashboard) y las cards de "top categoría" — ya no filtran a `['COP','USD']`, muestran cualquier moneda que el usuario realmente tenga en sus datos (derivado de las respuestas de `/summary`, `/summary-extra/*`, no de una lista fija).

`formatCurrency` (`src/lib/format.ts`) usa `Intl.NumberFormat` con `style: 'currency'` en vez de un mapa manual de símbolos — antes mostraba siempre 0 decimales y el mismo `$` plano para COP y USD (ocultaba los centavos en USD y era ambiguo entre monedas); ahora Intl resuelve símbolo y decimales correctos por código ISO.

## Features principales

- **Resumen** (`(app)/summary`): combina 7 hooks independientes en paralelo (`useSummary`, `useAssetsSummary`, `useLiabilitiesSummary`, `useNetWorthSummary`, `useCashFlowSummary`, `useBudgets`, `useNetWorthConsolidated`), cada uno con su propio fetch — sin caché compartida entre ellos. **Patrimonio neto consolidado** (desde 2026-08-30): tercera tarjeta "hero" junto a Patrimonio neto y Balance del período, en `User.report_currency` (configurable en Mi cuenta, no en el toggle de moneda de la propia página — son dos cosas independientes: el toggle cambia en qué moneda se lee *el resto* de Resumen, `report_currency` es la única moneda en la que tiene sentido *sumar* todas las demás). Clic en la tarjeta despliega una tabla de desglose por moneda (original, tasa usada, neto convertido) debajo de las 3 tarjetas hero. Si `GET /summary-extra/net-worth-consolidated` no pudo obtener la tasa de alguna moneda (`degraded: true`), esa fila del desglose queda con guiones y la tarjeta muestra un aviso ámbar — el total sigue siendo el de las monedas que sí se pudieron convertir, nunca rompe el resto de la página. Muestra KPIs, totales, gráfico de área (`AreaIncomeExpense`) y dos donuts por categoría (`DonutByCategory`). `SummaryLineChart.tsx` y `SummaryPieChart.tsx` existen pero **no se usan** en ninguna página (código muerto/iteración anterior). **"Presupuestos del mes"** (desde 2026-08-30): sección con barra de progreso por categoría, filtrada a la `currency` seleccionada en el toggle de la página (nunca fusiona montos entre monedas) — a diferencia del resto de Resumen, siempre es del mes en curso, no respeta el `DateRangePicker` (`useBudgets`/`GET /budgets` no acepta rango, solo `month`). Se oculta por completo si el usuario no tiene presupuestos en esa moneda (sin estado vacío en el dashboard). Si algún presupuesto llega a 100%, aparece un banner rojo arriba con el mismo estilo que el banner de sobregasto (`s.overspending_alert`) que ya existía más abajo. El tono de color por porcentaje (`progressTone`, `lib/budgetDisplay.ts`) es compartido con la pestaña `/budgets` para que ambas vistas se vean iguales.
- **Transacciones** (`(app)/transactions`): tabla (`@tanstack/react-table` vía `data-table.tsx`) + vista mobile en cards. Reglas de edición/reversión repetidas en `columns.tsx` y `page.tsx`: editable solo si es manual (sin `source_type`, no cancelada, no reversión, no compra con tarjeta); reversible si no cancelada, no ya reversada, y no es `transfer`. Crear una transacción de tipo `expense` permite elegir entre cuenta bancaria o tarjeta de crédito (prefijo `debt-<id>` en el picker) — en ese caso se llama `POST /debts/{id}/purchase` en vez de `POST /transactions`. Botón "Repetir última" (desde 2026-08-28): busca la transacción manual más reciente (`GET /transactions/with-category?source=account`, filtrando client-side por `!source_type`) y abre `NewTransactionModal` precargado con sus datos (fecha reseteada a hoy) vía las props `initial`/`openSignal` — el usuario confirma antes de crear, no es un reenvío automático de un click. Hay dos instancias montadas de `NewTransactionModal` (desktop/mobile, mismo patrón que el resto de la toolbar) por lo que cada una tiene su propia señal de apertura para evitar que ambas se abran a la vez. **Transferencias fusionadas** (desde 2026-08-30): una transferencia crea 2 filas (pata `expense` + pata `income`, mismo `transfer_group_id`), que antes se mostraban como dos movimientos independientes. `mergeTransferPairs` (`lib/transactionDisplay.ts`) las colapsa client-side en una sola fila por página (tabla desktop y cards mobile) antes de renderizar; si las dos patas de un mismo grupo caen en páginas distintas de la paginación, no se fusionan y cada una se muestra sola (degradación aceptable, no un bug). Reversar sigue operando sobre la pata de salida (`id` de la fila fusionada) — el backend ya revierte la pata complementaria automáticamente, sin cambios ahí.
- **Cuentas de ahorro** (`(app)/saving-accounts`): agrupa por `type` (cash/bank/investment) + sección de cerradas. Política de acciones basada en si la cuenta tiene transacciones (`has-transactions`, cacheado localmente por cuenta): eliminar/editar-todo requieren "prístina" (sin transacciones), cerrar requiere balance 0, reabrir requiere estar cerrada. `WithdrawFromAccountModal.tsx` existe pero **no está enlazado a ningún botón** en la UI actual (el botón "Depositar" también está comentado en `AccountsSection.tsx`).
- **Deudas** (`(app)/debts`): agrupa por `kind` (loan/credit_card) + cerradas. "Pristina" = `transactions_count === 0`. Pagar (`PayDebtModal`) solo permite cuentas activas de la **misma moneda** que la deuda. "Agregar cargo" (`AddChargeToDebtModal`) incrementa el saldo sin tocar ninguna cuenta. **Ciclo de tarjeta de crédito** (desde 2026-08-30): `NewDebtModal`/`EditDebtModal` muestran un bloque adicional (cupo, día de corte 1-28, días para pagar, % de pago mínimo) solo cuando `kind=credit_card`, opcional al crear. `CreditCardCycleInfo` (`components/debts/`, con su propio hook `useDebtStatement`) se monta por tarjeta dentro de `DebtsSection` y llama `GET /debts/{id}/statement`; si la tarjeta no tiene el ciclo configurado el endpoint responde 400 y el componente simplemente no renderiza nada (no es un error visible). Muestra barra de cupo disponible (verde/ámbar/rojo según % usado) y cuenta regresiva a la fecha de pago (mismo esquema de color por proximidad — rojo ≤3 días o vencido, ámbar ≤7). El pago mínimo siempre se etiqueta "estimado".
- **Categorías** (`(app)/categories`): lista activas/inactivas; las de sistema (`is_system`) están bloqueadas para editar/desactivar desde la UI. Desactivar = soft-delete (`DELETE`), con confirmación vía `ConfirmCategoryStatusModal`.
- **Presupuestos** (`(app)/budgets`, desde 2026-08-30; accesible como pestaña "Presupuestos" en `/categories` vía `CategoriesTabs.tsx`): meta de gasto mensual por categoría de egreso y moneda (`useBudgets` hook, `GET /budgets` ya trae `spent`/`percentage` calculados por el backend — el frontend no calcula gasto real). Tarjetas con barra de progreso (verde <80%, ámbar 80-100%, rojo ≥100% + badge "Presupuesto superado"), ordenadas por `percentage` descendente. `BudgetModal` crea/edita: en modo edición, categoría y moneda quedan bloqueadas (`POST /budgets` es un upsert por categoría+moneda+mes — cambiar cualquiera de las dos crearía un presupuesto distinto, no editaría el actual) y solo se puede tocar el monto. "Pausar" llama `POST /budgets/{id}/pause`, que oculta esa categoría de la lista desde el mes en curso sin borrar el histórico.
- **Reglas de categorización** (`(app)/rules`, desde 2026-08-30; pestaña "Reglas" en `/categories` vía `CategoriesTabs.tsx`): si la descripción de una transacción contiene un texto, sugiere una categoría — texto-contiene, sin distinguir mayúsculas, sin regex. Se evalúan en el orden en que aparecen en la lista (`priority` ascendente en el backend), gana la primera que matchea; reordenar con las flechas ↑/↓ intercambia el `priority` de la fila movida con el de su vecina (dos `PUT` en paralelo) en vez de un endpoint dedicado de reordenamiento. "Aplicar a existentes" llama `POST /category-rules/apply`, que recategoriza las transacciones que **todavía** están en "Sin categorizar" (no toca las que ya tienen otra categoría). Atajo "crear regla desde esta transacción": un botón con ícono de etiqueta en la columna de acciones de `(app)/transactions` (**solo en la tabla de escritorio**, no en las cards de mobile — limitación de alcance de esta fase) abre `RuleModal` precargado con la descripción y categoría de esa fila. Alcance de v1, a propósito: sin autosugerencia mientras se escribe la descripción en el formulario manual de nueva transacción.
- **Importar CSV** (`(app)/import`, desde 2026-08-30; sin ítem en el sidebar, se llega vía el botón "Importar CSV" en `/saving-accounts`): wizard de 4 pasos en un solo componente (`step: 'select'|'mapping'|'review'|'done'`), sin hook propio. 1) elegir cuenta destino + archivo; llama `POST /transactions/import/preview` **sin** `column_mapping` (modo `inspect`) para traer una muestra cruda y el perfil guardado de esa cuenta si existe. 2) mapear qué columna (por índice, no por nombre de encabezado) es fecha/descripción/monto + formato de fecha (`strptime`, ej. `%d/%m/%Y`) + si la primera fila es encabezado; checkbox "Recordar este mapeo" hace `POST /import-profiles` al continuar. 3) llama `preview` otra vez, ahora **con** mapeo (modo `review`): tabla editable con checkbox de inclusión por fila (los duplicados y las filas con error vienen desmarcados por defecto, estas últimas además deshabilitadas), un `Select` de categoría por fila (sugerida por `/category-rules` si alguna coincide con la descripción, si no "Sin categorizar" — se refetchea `/categories` después del parse porque esa categoría de sistema se autocrea recién en esa llamada, si el usuario no la tenía antes no existía todavía cuando la página cargó las categorías por primera vez), badges de estado (Lista/Posible duplicado/Error), y acciones masivas (marcar/desmarcar todas, ignorar duplicados). 4) `POST /transactions/import/confirm` con solo las filas incluidas → resultado `{created, skipped}`. Introduce `components/ui/checkbox.tsx`, un checkbox nativo (no hay `@radix-ui/react-checkbox` entre las dependencias) con la misma API `checked`/`onCheckedChange` que el resto del sistema de diseño.
- **Recurrentes** (`(app)/recurring`, desde 2026-08-22; accesible como pestaña "Recurrentes" en `/transactions` desde 2026-08-30): plantillas de movimientos que se repiten (nómina, arriendo, suscripciones). Lista activas/pausadas con su próxima fecha, marca las vencidas, y permite editar/pausar/eliminar. Botón "Registrar pendientes" → `POST /recurring-transactions/run`. Además, el layout `(app)` dispara esa misma generación **una vez por sesión** (flag en `sessionStorage`) para que no dependa de que el usuario visite la sección; es silenciosa si no hay nada que hacer, y los avisos de omitidos (típicamente saldo insuficiente) duran más en pantalla porque son los que requieren acción.
- **Registro rápido (quick-add)** (desde 2026-08-28): FAB verde fijo (`QuickAddFab.tsx`, bottom-right) montado en `(app)/layout.tsx` — visible en cualquier pantalla protegida, no solo `/transactions`. Reusa `NewTransactionModal` con `hideTrigger` (oculta su botón "+ Nueva Transacción" propio) y `openSignal` para abrirlo en blanco. Al crear, hace `window.location.reload()` en vez de un refresh selectivo: no hay caché de datos compartida entre features que invalidar, así que recargar es la forma simple de garantizar que la pantalla actual (resumen, cuentas, deudas...) refleje el movimiento nuevo.
- **Administración** (`(app)/admin`, solo admins, desde 2026-08-22): lista paginada de usuarios con buscador por correo (con debounce de 350ms), mostrando rol y estado de suscripción de cada uno. Acciones: gestionar suscripción (`ManageSubscriptionModal` → crear/renovar/eliminar, contra `/subscriptions/admin/*`) y promover/degradar admins (`PATCH /admin/users/{id}/role`). El enlace en el sidebar solo aparece si `useCurrentUser().isAdmin`; el acceso real lo hace cumplir el backend (403). El backend rechaza quitar el último admin, y la UI lo muestra como error normal.

## Utils compartidos (`src/lib/`)

| Archivo | Propósito |
|---|---|
| `format.ts` | `formatCurrency` |
| `formatDate.ts` | formatea ISO → timezone del navegador (Luxon) |
| `formatDayLabel.ts` | igual, pero evita conversión de timezone si el input es un "día lógico" `YYYY-MM-DD` sin hora (para no correr un día las etiquetas de gráficos) |
| `date.ts` | `toLocalDateString` |
| `dateParams.ts` | construcción de query params de fecha (el más cargado; ver sección de fechas arriba) |
| `extractErrorMessage.ts` | desempaqueta errores de Axios/Pydantic (string, array de validación v2, `detail.message`) — usado en algunos hooks, pero varios modales (`NewDebtModal`, `EditDebtModal`, `AddChargeToDebtModal`, `CategoryModal`) reimplementan su propia versión local en vez de importar esta |
| `utils.ts` | `cn()` (clsx + tailwind-merge) |
| `transactionDisplay.ts` (desde 2026-08-28, extendido 2026-08-30) | Helpers de presentación de transacciones (`typeColor`, `isCreditCardPurchase`, `isTransferLeg`, `getTxCurrency`, `getStatusLabel`, `categoryBadgeClasses`) compartidos entre `(app)/transactions/columns.tsx`, `(app)/transactions/page.tsx` (vista mobile) y `AccountTransactionsModal.tsx`. Desde 2026-08-30 también trae `mergeTransferPairs`/`transferDisplayDescription`/`transferAmountDisplay` para colapsar las 2 patas de una transferencia en una sola fila. |
| `colorRoles.ts` (desde 2026-08-29) | Mapa de color único por rol semántico (`positive`/`negative`/`warning`/`info`/`creditCard`/`neutral` → variante de `Button`). Fuente del diagnóstico de diseño: antes, botones como "Ver movimientos"/"Pagar" cambiaban de color según el *tipo* de cuenta/deuda (emerald/sky/violet, amber/fuchsia) en vez de según la *acción*. Adoptado en `AccountsSection.tsx` y `DebtsSection.tsx`; pendiente en el resto de componentes que aún fijan color a mano. |
| `budgetDisplay.ts` (desde 2026-08-30) | `progressTone(percentage)` — verde/ámbar/rojo (cortes en 80% y 100%) para la barra y el badge de un presupuesto. Compartido entre `(app)/budgets/page.tsx` y la sección "Presupuestos del mes" de `(app)/summary/page.tsx`. |

## Sistema de diseño (`src/components/ui/`, desde 2026-08-29)

Diagnóstico de diseño (navegación + identidad visual): el problema no era la ausencia de tokens/variantes (`globals.css` ya define colores semánticos y `Card`/`Button` ya traían variantes completas) sino que cada uno de los ~20 modales y 7 páginas los ignoraba y resolvía su propio color/chrome a mano. Fases A, B y C completas (2026-08-29/30) — ver `backend/docs/PENDIENTES.md` para el detalle de cada fase.

- **`form-modal.tsx`** — `<FormModal>` envuelve `Dialog`/`DialogContent` y resuelve header+body+footer a partir de un solo `tone` (`neutral`/`emerald`/`rose`/`sky`/`amber`/`fuchsia`), en vez de que cada modal calcule su propio `panelTint`/`headerFooterTint`. No asume la lógica de envío del formulario — el `footer` (Cancelar + CTA) lo sigue armando cada modal, y es opcional (modales de solo lectura como `AccountTransactionsModal`/`DebtTransactionsModal` no lo pasan). **Los 20 modales que tenían el patrón manual ya están migrados** — no queda ninguno con `panelTint`/`headerFooterTint` propio. `WithdrawFromAccountModal.tsx` es la única excepción deliberada: no está enlazado a ningún botón (ver Fase 5 en PENDIENTES.md), migrarlo sería pulir código muerto.
- **`page-header.tsx`** — `<PageHeader title subtitle actions align />` reemplaza el `<h1 className='text-2xl font-semibold'>` + subtítulo que cada página copiaba a mano. `align='end'` (default) apila en mobile y alinea las acciones al final del bloque título+subtítulo en desktop — para acciones "altas" (buscador, date range picker): `summary`, `categories`, `admin`, `recurring`. `align='center'` es una fila única centrada — para uno o dos botones simples: `debts`, `saving-accounts`. Adoptado en las 7 páginas de `(app)`.
- Corregido de paso (hallazgos de severidad alta del diagnóstico): en `(app)/summary/page.tsx`, "Total deudas" ahora usa `variant='kpi-expense'` (antes `kpi-income`, verde, pese a ser un pasivo — inconsistente con el rojo que ya usaba `/debts` para el mismo total) y "Patrimonio neto" ahora elige `kpi-income`/`kpi-expense` según el signo (antes siempre verde, incluso en negativo), mismo patrón que ya usaba "Flujo neto" un poco más abajo en el propio archivo.
- **`colorRoles.ts` adoptado en `AccountsSection.tsx`/`DebtsSection.tsx`**: "Ver movimientos" siempre `info` (sky), "Editar" siempre `neutral` (slate), "Pagar"/"Agregar rendimiento" siempre `positive` (emerald) — sin importar si la cuenta es efectivo/banco/inversión o si la deuda es préstamo/tarjeta. El borde de la card (`toneBorderMap`) sí sigue variando por tipo a propósito (agrupación visual, no es una acción).
- **Jerarquía de KPI en `(app)/summary/page.tsx`** (rediseñado 2026-08-29): antes 10 tarjetas del mismo peso visual. Ahora 2 tarjetas "hero" grandes (`text-3xl`, gradiente `kpi-income`/`kpi-expense`/`kpi-balance`) para Patrimonio neto y Balance del período, + 8 métricas secundarias en una grilla `grid-cols-2 sm:grid-cols-4` de tarjetas `variant='surface'` pequeñas y silenciosas, donde el color solo vive en el texto del valor (`text-emerald-700`/`text-rose-700`/`text-amber-700`), no en el fondo.
- **FAB de registro rápido vs. contenido**: `(app)/layout.tsx` agrega `pb-20` (mobile) al contenedor de contenido para que el FAB fijo no tape el final de la página justo antes del footer. La jerarquía de KPI de arriba también ayuda: antes el FAB podía solaparse con "Total deudas" en la carga inicial en mobile; ahora esa tarjeta bajó de prioridad visual y las 2 tarjetas hero quedan siempre fuera de esa esquina.
- **KPI clicables en `(app)/summary/page.tsx`**: "Total en cuentas" y "Total deudas" (grilla secundaria) enlazan a `/saving-accounts` y `/debts` respectivamente (`Card` envuelto en `next/link`, `interactive` para el hover). El resto de las tarjetas no enlaza a ningún lado: no hay una página 1:1 razonable para "Flujo neto" o "Ingresos de caja" sin forzar el mapeo.
- **Rebrand a "Balanced Cent"** (2026-08-30): el sidebar mostraba "Finanzas / Personal" con ₿ como logomark — una app de finanzas fiat multi-moneda que nunca menciona cripto, resto de placeholder de antes de bautizar el producto. Reemplazado por un logomark tipográfico ("B" en `--sidebar-primary`) + "Balanced Cent" en `Sidebar.tsx` (desktop y overlay mobile, vía un `BrandMark` compartido), `Footer.tsx`, `<title>` (`app/layout.tsx`), y el texto de `/auth/expired`. `NEXT_PUBLIC_APP_NAME` (`.env.local`/`.env.example`) actualizado a `Balanced Cent` — **falta actualizarlo también en Vercel (producción)**, ver PENDIENTES.md.
- **Footer recortado** (`Footer.tsx`, 2026-08-30): antes 3 columnas (marca + blurb + CTA "Conectar cuentas" que no correspondía a ninguna función real — la app no tiene integración bancaria — + lista "Módulos" que duplicaba el sidebar + "Acciones rápidas"). Ahora una sola línea: © + nombre + versión + disclaimer. Coherente con que un shell de producto autenticado (no una landing) no necesita repetir la navegación que ya está en el sidebar.

## Componentes de layout con posible código huérfano

- `Header.tsx` no se usa en `(app)/layout.tsx` (que renderiza `Sidebar` + dos FABs propios: toggle de sidebar en mobile y registro rápido) — sigue sin usarse, pero ya no duplica lógica de logout (ambos llaman a `logout()` de `src/lib/api.ts`).
- `MobileSidebarTrigger.tsx` parece duplicar el FAB ya codificado inline en `(app)/layout.tsx`.

Vale la pena confirmar con una búsqueda de imports antes de eliminar cualquiera de los dos, por si se usan en algún lugar no cubierto en este análisis.
